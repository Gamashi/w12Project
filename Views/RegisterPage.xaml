<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="w12.Views.RegisterPage"
             BackgroundColor="DimGray"
             Title="RegisterPage">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="7*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <Label Text="Seja Bem Vindo!" TextColor="White" FontAttributes="Bold"/>
            <Label Text="Primeiro, vamos realizar o seu cadastro" TextColor="White" Grid.Row="1" FontAttributes="Bold"/>
        </Grid>


        <Grid Grid.Row="1" >
            
        </Grid>
        
    </Grid>
</ContentPage>